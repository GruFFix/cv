import s from './Footer.module.scss'

export const Footer = () => {
  return (
    <footer className={s.root}>
      <p>last operation logs operations here. rollup to up.</p>
      <p>© ProtectedPool</p>
    </footer>
  )
}
